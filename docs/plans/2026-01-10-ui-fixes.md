# UI and Design Refinement Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Use the `frontend-design` skill for all UI changes.

**Goal:** Refine the landing page UI to be more business-oriented, transparent, and functional, fixing layout bugs and improving content relevance for the Russian market.

**Architecture:** Maintain React + CSS Modules + Framer Motion. Refactor components for better responsiveness and clarity.

**Tech Stack:**
- React 18
- Tailwind CSS
- CSS Modules
- Framer Motion
- Lucide React

---

## Task 1: Header & Typography Foundation

**Files:**
- Modify: `src/components/Header.tsx`
- Modify: `src/components/Header.module.css`
- Modify: `tailwind.config.js`
- Modify: `src/index.css`

**Steps:**
1. **Typography**: Add a more business-like font (e.g., 'Golos Text' or 'Manrope') to `tailwind.config.js` and `index.css`. Replace `Inter` and the current serif with more professional pairings.
2. **Header Fix**: Fix the background transition bug. Ensure the header doesn't "jump" or overlap Hero incorrectly. Remove `ARCHITECTURAL` from the logo.
3. **Remove English**: Translate `Connection`, `Location`, `Membership`, etc. in Header and Footer.

## Task 2: Hero Section Optimization

**Files:**
- Modify: `src/components/Hero.tsx`
- Modify: `src/components/Hero.module.css` (if exists, or add styles inline/via tailwind)

**Steps:**
1. **Height**: Set section to `min-h-[100svh]` and ensure all content fits without scrolling on most screens.
2. **Overflows**: Adjust the visual side container (aspect ratio and margins) to prevent elements from exceeding their containers.
3. **Background**: Remove the large background "OTIS" text.
4. **Content**: Ensure all text is in Russian.

## Task 3: Benefits (Bento Grid) Refactor

**Files:**
- Modify: `src/components/Benefits.tsx`

**Steps:**
1. **Content**: Reduce blocks. Keep: 10% commission, Calculation in 15 min, Turnkey installation, Personal manager.
2. **Removal**: Remove "бесплатный замер", "поддержка" (merged with manager), "рассрочка".
3. **Design**: Reduce height of the 10% block by half. Make the "10%" text much larger and more expressive.
4. **Animation**: Remove "blinking" effects (excessive animations).

## Task 4: Process & Partition Types

**Files:**
- Modify: `src/components/HowItWorks.tsx`
- Create: `src/components/PartitionTypes.tsx`
- Modify: `src/App.tsx`

**Steps:**
1. **Process**: Rename steps to be transparent: "Заявка", "Расчет", "Производство", "Монтаж и Бонус".
2. **New Block**: Create a section showing partition types (Sliding, Folding, Stationary, Loft) based on `otisperegorodki.ru` info.

## Task 5: Tariffs & Projects Refinement

**Files:**
- Modify: `src/components/PartnershipTerms.tsx`
- Modify: `src/components/Projects.tsx`

**Steps:**
1. **Tariffs**: Merge 3 tiers into one clear "Partner Offer" with a list of benefits for different partner types.
2. **Projects**: Remove grayscale filter. Add the new images from `public/images/partitions`. Ensure labels are in Russian.

## Task 6: Testimonials & Footer

**Files:**
- Modify: `src/components/Testimonials.tsx`
- Modify: `src/components/Footer.tsx`

**Steps:**
1. **Testimonials**: Shrink photos. Add 2 more Slavic personas. Use local names (e.g., Елена Соколова, Игорь Кузнецов).
2. **Footer**: Remove `ARCHITECTURAL`. Ensure all links are in Russian. Fix contact info.

## Task 7: Final Polish & Vercel Verification

**Steps:**
1. Run `npm run build` to ensure no regressions.
2. Verify mobile responsiveness one last time.
3. Final commit and push.
